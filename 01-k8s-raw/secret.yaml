# Sensitive config for hello. Same copy-paste-per-environment problem as
# every other file here — plus a reminder for later: Secret data below is
# just base64, not encrypted (revisited in the "状态与配置" section).
apiVersion: v1
kind: Secret
metadata:
  name: hello-db
type: Opaque
data:
  # echo -n 'changeme' | base64
  password: Y2hhbmdlbWU=
