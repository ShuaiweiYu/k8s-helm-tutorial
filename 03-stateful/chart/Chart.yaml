# Objects are named after the release; see 02-helm-basic/chart/Chart.yaml.
apiVersion: v2
name: hello
description: hello chart, evolved with an optional Postgres backend for the stateful/PVC demo.
type: application
version: 0.2.0
appVersion: "v1"
