# See 02-helm-basic/chart/Chart.yaml for why this is named "hello".
apiVersion: v2
name: hello
description: hello chart, evolved with a ConfigMap to demo the "envFrom doesn't hot-reload" pitfall.
type: application
version: 0.3.0
appVersion: "v1"
